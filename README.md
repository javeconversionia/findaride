# Find-A-Ride

Next.js app using the App Router. Root-level files:
- `app/` — app routes and server components
- `components/` — UI components
- `lib/` — simple data functions
- `styles/` — CSS

Local:
1. `npm install`
2. `npm run dev`
Build:
1. `npm run build`
2. `npm run start`
